# Benchmark Results

**Date:** 2026-02-04 05:11:28

**Method:** Cold cache (PostgreSQL restarted before each query)

## Argentina

**Config:** `{"id": "6aaadea0-9c48-4af0-a47f-bbe020540580", "name": "Argentina"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 453744.7ms

```
count
-----
822527
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '6aaadea0-9c48-4af0-a47f-bbe020540580';
```

**Time:** 436492.4ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=436481.284..436481.285 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=130 width=0) (actual time=1123.684..436347.442 rows=822527 loops=1)
        ->  Index Scan using divisions_pkey on divisions d  (cost=0.43..8.45 rows=1 width=5558) (actual time=0.039..0.041 rows=1 loops=1)
              Index Cond: (id = '6aaadea0-9c48-4af0-a47f-bbe020540580'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=1118.331..436242.415 rows=822527 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 625443
Planning Time: 8.308 ms
Execution Time: 436482.199 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 26658.3ms

```
count
-----
827138
```

## Deutschland

**Config:** `{"id": "d95d3f8a-a2f4-4436-b0e4-a3a86d15008e", "name": "Deutschland"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 2499.9ms

```
count
-----
3150298
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e';
```

**Time:** 2705.0ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=2693.689..2693.690 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=130 width=0) (actual time=10.319..2602.440 rows=3150298 loops=1)
        ->  Index Scan using divisions_pkey on divisions d  (cost=0.43..8.45 rows=1 width=5558) (actual time=0.045..0.047 rows=1 loops=1)
              Index Cond: (id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=6.312..2454.733 rows=3150298 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 906565
Planning Time: 9.324 ms
Execution Time: 2694.354 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 4721.4ms

```
count
-----
3168754
```

## United States

**Config:** `{"id": "50a03a2c-3e24-4740-b80d-f933ea60c64f", "name": "United States"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 27538.2ms

```
count
-----
15506452
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '50a03a2c-3e24-4740-b80d-f933ea60c64f';
```

**Time:** 29018.5ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=29008.000..29008.001 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=130 width=0) (actual time=9.012..28502.677 rows=15506452 loops=1)
        ->  Index Scan using divisions_pkey on divisions d  (cost=0.43..8.45 rows=1 width=5558) (actual time=0.045..0.048 rows=1 loops=1)
              Index Cond: (id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=5.169..27760.944 rows=15506452 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 37116802
Planning Time: 8.580 ms
Execution Time: 29008.757 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 114968.6ms

```
count
-----
15547987
```
