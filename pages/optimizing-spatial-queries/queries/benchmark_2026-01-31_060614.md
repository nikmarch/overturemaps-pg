# Benchmark Results

**Date:** 2026-01-31 06:06:14

**Method:** Cold cache (PostgreSQL restarted before each query)

## Argentina

**Config:** `{"id": "6aaadea0-9c48-4af0-a47f-bbe020540580", "name": "Argentina"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 898064.3ms

```
count
-----
822527
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '6aaadea0-9c48-4af0-a47f-bbe020540580';
```

**Time:** 901684.3ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=901614.128..901614.221 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=2218.104..901400.788 rows=822527 loops=1)
        ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=0.717..0.726 rows=1 loops=1)
              Index Cond: (id = '6aaadea0-9c48-4af0-a47f-bbe020540580'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=2211.676..901121.463 rows=822527 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 625443
Planning Time: 45.629 ms
Execution Time: 901617.004 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 55421.3ms

```
count
-----
827138
```

## Deutschland

**Config:** `{"id": "d95d3f8a-a2f4-4436-b0e4-a3a86d15008e", "name": "Deutschland"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 4782.4ms

```
count
-----
3150298
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e';
```

**Time:** 4144.4ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=4085.643..4085.688 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=9.703..3932.884 rows=3150298 loops=1)
        ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=0.645..0.649 rows=1 loops=1)
              Index Cond: (id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=6.315..3683.658 rows=3150298 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 906565
Planning Time: 39.859 ms
Execution Time: 4087.905 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 6476.7ms

```
count
-----
3168754
```

## United States

**Config:** `{"id": "50a03a2c-3e24-4740-b80d-f933ea60c64f", "name": "United States"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 77345.8ms

```
count
-----
15506452
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '50a03a2c-3e24-4740-b80d-f933ea60c64f';
```

**Time:** 96900.8ms

```
QUERY PLAN
----------
Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=96786.784..96786.843 rows=1 loops=1)
  ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=30.398..95989.805 rows=15506452 loops=1)
        ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=1.723..1.760 rows=1 loops=1)
              Index Cond: (id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'::text)
        ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=8.645..94694.429 rows=15506452 loops=1)
              Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
              Filter: st_covers(d.geometry, geometry)
              Rows Removed by Filter: 37116802
Planning Time: 71.105 ms
Execution Time: 96789.727 ms
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 249490.2ms

```
count
-----
15547987
```
