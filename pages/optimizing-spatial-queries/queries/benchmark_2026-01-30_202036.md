# Benchmark Results

**Date:** 2026-01-30 20:20:36

**Method:** Cold cache (PostgreSQL restarted before each query)

## Argentina

**Config:** `{"id": "6aaadea0-9c48-4af0-a47f-bbe020540580", "name": "Argentina"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 886343.4ms

```
count  
--------
 822527
(1 row)
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '6aaadea0-9c48-4af0-a47f-bbe020540580';
```

**Time:** 884015.3ms

```
QUERY PLAN                                                                        
---------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=883637.293..883637.365 rows=1 loops=1)
   ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=2175.485..883465.193 rows=822527 loops=1)
         ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=0.676..0.686 rows=1 loops=1)
               Index Cond: (id = '6aaadea0-9c48-4af0-a47f-bbe020540580'::text)
         ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=2168.732..883235.828 rows=822527 loops=1)
               Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
               Filter: st_covers(d.geometry, geometry)
               Rows Removed by Filter: 625443
 Planning Time: 42.008 ms
 Execution Time: 883640.117 ms
(10 rows)
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '6aaadea0-9c48-4af0-a47f-bbe020540580'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 53481.9ms

```
count  
--------
 827138
(1 row)
```

## Deutschland

**Config:** `{"id": "d95d3f8a-a2f4-4436-b0e4-a3a86d15008e", "name": "Deutschland"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 6413.1ms

```
count  
---------
 3150298
(1 row)
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e';
```

**Time:** 4421.6ms

```
QUERY PLAN                                                                      
-----------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=4075.318..4075.349 rows=1 loops=1)
   ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=9.614..3922.290 rows=3150298 loops=1)
         ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=0.653..0.656 rows=1 loops=1)
               Index Cond: (id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'::text)
         ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=6.124..3677.978 rows=3150298 loops=1)
               Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
               Filter: st_covers(d.geometry, geometry)
               Rows Removed by Filter: 906565
 Planning Time: 37.924 ms
 Execution Time: 4077.587 ms
(10 rows)
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = 'd95d3f8a-a2f4-4436-b0e4-a3a86d15008e'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 6786.4ms

```
count  
---------
 3168754
(1 row)
```

## United States

**Config:** `{"id": "50a03a2c-3e24-4740-b80d-f933ea60c64f", "name": "United States"}`

### cte

```sql
-- Using MATERIALIZED CTE
WITH div AS MATERIALIZED (
    SELECT geometry as geom FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 77402.6ms

```
count   
----------
 15506452
(1 row)
```

### join

```sql
EXPLAIN ANALYZE
SELECT COUNT(*) FROM places p
JOIN divisions d ON p.geometry && d.geometry AND ST_Covers(d.geometry, p.geometry)
WHERE d.id = '50a03a2c-3e24-4740-b80d-f933ea60c64f';
```

**Time:** 72057.2ms

```
QUERY PLAN                                                                       
-------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=29.72..29.73 rows=1 width=8) (actual time=71568.305..71568.374 rows=1 loops=1)
   ->  Nested Loop  (cost=0.85..29.40 rows=128 width=0) (actual time=32.289..70805.382 rows=15506452 loops=1)
         ->  Index Scan using divisions_id_idx on divisions d  (cost=0.43..8.45 rows=1 width=5488) (actual time=1.693..1.735 rows=1 loops=1)
               Index Cond: (id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'::text)
         ->  Index Scan using places_geometry_idx on places p  (cost=0.42..20.94 rows=1 width=32) (actual time=8.870..69575.604 rows=15506452 loops=1)
               Index Cond: ((geometry && d.geometry) AND (geometry @ d.geometry))
               Filter: st_covers(d.geometry, geometry)
               Rows Removed by Filter: 37116802
 Planning Time: 62.854 ms
 Execution Time: 71571.121 ms
(10 rows)
```

### simplified

```sql
-- Using MATERIALIZED CTE with ST_Simplify + ST_Buffer
WITH div AS MATERIALIZED (
    SELECT ST_Buffer(ST_Simplify(geometry, 0.01), 0.01) as geom
    FROM divisions WHERE id = '50a03a2c-3e24-4740-b80d-f933ea60c64f'
)
SELECT COUNT(*) FROM places p, div
WHERE p.geometry && div.geom
  AND ST_Covers(div.geom, p.geometry);
```

**Time:** 251060.9ms

```
count   
----------
 15547987
(1 row)
```
